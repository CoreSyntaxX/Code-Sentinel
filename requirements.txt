# For AST parsing (optional)
esprima>=4.0.1

# For YAML parsing (for rules)
pyyaml>=6.0

# For web crawling (future)
requests>=2.28.0
beautifulsoup4>=4.11.0

# For GitHub API (future)
pygithub>=1.55

# Note: concurrent-futures is a built-in Python module (part of standard library since Python 3.2)
